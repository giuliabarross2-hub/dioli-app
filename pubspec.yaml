name: dioli_agenda
description: Agenda interna do DIOLI - Studio de Beleza
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  google_sign_in: ^7.2.0
  google_sign_in_web: ^1.1.3
  googleapis: ^17.0.0
  http: ^1.6.0
  intl: ^0.20.3
  shared_preferences: ^2.5.3
  supabase_flutter: ^2.17.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  fonts:
    - family: BostonAngel
      fonts:
        - asset: Boston Angel Regular.otf
